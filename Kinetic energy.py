# Get user input for mass and velocity of the object
mass = float(input("Enter the mass (in kilograms): "))
velocity = float(input("Enter the velocity(in m/s): "))
Kinetic Energy (KE) = 0.5 * mass * velocity^2
